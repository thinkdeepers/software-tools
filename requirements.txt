# 简压依赖。
#
# 核心压缩（无密码 ZIP）仅使用 Python 标准库。
# 下列依赖用于扩展格式与加密，安装版打包时会一并打入 exe。

py7zr>=0.20          # 7z 解压 / 加密 7z 读取
rarfile>=4.0         # rar 解压（需 UnRAR 工具，安装版已捆绑）
pyzipper>=0.3.6      # AES 加密 ZIP 的创建与解压

# 打包为 Windows 可执行文件（开发时使用）：
# pyinstaller>=6.0
